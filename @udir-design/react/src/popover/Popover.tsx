import {
  Popover,
  type PopoverProps,
  PopoverContext,
  type PopoverContextProps,
  PopoverTrigger,
  type PopoverTriggerProps,
} from '@digdir/designsystemet-react';

export {
  Popover,
  PopoverProps,
  PopoverContext,
  PopoverContextProps,
  PopoverTrigger,
  PopoverTriggerProps,
};
