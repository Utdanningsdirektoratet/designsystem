import { NumberOfRows } from './examples/NumberOfRows';
import { Meta } from '@storybook/addon-docs/blocks';
import { Unstyled } from '@storybook/addon-docs/blocks';

<Unstyled>
<Meta title="patterns/Tabeller" tags={['alpha']} />

# Tabeller

## Visning av rader i tabell

Når det er mange rader vil man kanskje ikke vise alle på en gang.

<NumberOfRows />
</Unstyled>
