import { Meta, Canvas, Controls, Primary } from '@storybook/addon-docs/blocks';
import * as BadgeStories from './Badge.stories';

<Meta of={BadgeStories} />

# Badge

`Badge` er ein ikkje-interaktiv komponent som viser status med eller utan tal.

## Slik bruker du Badge

**Passer til:**

- Tal som viser antall nye meldingar, varsel eller oppgåver.
- Små statusindikatorar i form av ein sirkel, som for eksempel viser om ein person er opptatt, borte eller aktiv.

**Passer ikkje til:**

- Tekstar - bruk [`Tag`](/docs/components-tag--docs) i staden.
- Interaktive handlingar - bruk [`Chip`](/docs/components-chip--docs) eller [`Button`](/docs/components-button--docs) i staden.

<Primary />
<Controls />

```tsx
import { Badge } from '@udir-design/react';

/* Inline */
<Badge />

/* Flytende over element */
<Badge.Position>
  <Badge />
  <Elem />
</Badge.Position>
```

## Variantar av Badge

### Fargar

`Badge` finst i alle fargar. Du kan endre fargen med `data-color`.
Du kan også setje `variant` direkte for å endre bakgrunnsfargen.

<Canvas of={BadgeStories.Variants} />

### Vise staus

Dersom du ikkje sender inn `count`, blir berre ein sirkel vist. Dette kan brukast for å vise status.

<Canvas of={BadgeStories.Status} />

## Eksempler

### Flytande over element

Du kan setje plassering med `placement`. Dersom elementet du plasserer over er ein sirkel, kan du setje `overlap="circle"` for at badgen skal flyte over elementet.

<Canvas of={BadgeStories.Floating} />

Ikkje alle ikon har lik form, og då må du kanskje endre kor `Badge` skal plasserast. Dersom du ikkje får `Badge` til å liggje der du ynskjer, kan du bruke `style` for å overstyre plasseringa.
Dette fungerer også når du bruker `overlap="circle"`.

<Canvas of={BadgeStories.CustomPlacement} />

### Inline

Du kan også bruke komponenten inline.

<Canvas of={BadgeStories.InTabs} />

## Retningslinjer

Bruk `Badge` for å trekkje merksemd mot statusar, varslar eller tal.

## Tekst

Denne komponenten er ikkje meint for tekst. Viss du treng å vise ord eller korte frasar, bør du heller bruke [`Tag`](/docs/components-tag--docs)-komponenten.

## Tilgjengeligheit

Sørg for at informasjonen også er tilgjengeleg for skjermlesarar. [Meir informasjon kjem.](https://github.com/digdir/designsystemet/issues/3560)
