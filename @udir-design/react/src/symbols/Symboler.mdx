import {
  Meta,
  Canvas,
  Controls,
  IconGallery,
  ArgTypes,
} from '@storybook/addon-docs/blocks';
import React from 'react';
import * as SymbolsStories from './Symbols.stories';
import { Unstyled } from '@storybook/addon-docs/blocks';
import { SymbolDisplay } from './docs/SymbolDisplay';
import { HideToc } from '.storybook/docs/components';

<HideToc />

# Symboler

Symboler gir visuell støtte til å oppfatte et innhold og bør stå sammen med tekst. De er tilgjengelig både som React-komponenter og som svg-filer. Vi anbefaler å bruke React-komponentene når du bruker React. Trykk på ett av symbolene i galleriet under for å se flere varianter og hvordan de brukes i kode.

I React endrer du størrelsen på ikonet ved å sette `size`-propen: `<BallongGrønnSymbol size="10rem" />`. Ved bruk av svg setter du `height` og/eller `width` med CSS: `.my-symbol { width: 10rem; }`.

<Meta of={SymbolsStories} />

<Unstyled>
  <SymbolDisplay />
</Unstyled>
