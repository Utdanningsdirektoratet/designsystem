# Lokal kjøring av CodeQL

For å kunne kjøre CodeQL lokalt må du ha CLI-kommandoen installert. På macOS med Homebrew:

```
brew install codeql
```

Deretter kan du kjøre `pnpm codeql` i rot av repoet for å kjøre code scanning
