{
  "name": "@udir-design/tokens",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./dist/udir.css",
    "./*": "./dist/*"
  },
  "scripts": {
    "create": "designsystemet tokens create",
    "build": "pnpm clean && designsystemet tokens build -t ./src -o ./dist && pnpm build:data-css",
    "clean": "rimraf dist",
    "build:data-css": "node process/build-data-css.mjs"
  },
  "devDependencies": {
    "@digdir/designsystemet": "catalog:",
    "@tokens-studio/sd-transforms": "catalog:",
    "rimraf": "catalog:",
    "style-dictionary": "catalog:"
  },
  "dependencies": {
    "ramda": "catalog:"
  }
}
